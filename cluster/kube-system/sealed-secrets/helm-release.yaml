apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: sealed-secrets
  namespace: kube-system
spec:
  chart:
    spec:
      chart: sealed-secrets
      version: "1.13.x"
      sourceRef:
        kind: HelmRepository
        name: bitnami-sealed-secrets-charts
        namespace: flux-system
      interval: 5m
  releaseName: sealed-secrets
