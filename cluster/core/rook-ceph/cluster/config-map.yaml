apiVersion: v1
kind: ConfigMap
metadata:
  name: rook-config-override
  namespace: rook-ceph
data:
  config: |
    [global]
    mon clock drift allowed = 0.8
    # config for 2 node cluster
    osd pool default size = 2
    osd_pool_default_pg_num = 128
    osd_pool_default_pgp_num = 128
