---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: jellyfin
  namespace: media
spec:
  entryPoints:
  - websecure
  routes:
  - match: Host(`jellyfin.${SECRET_DOMAIN}`)
    kind: Rule
    priority: 10
    services:
    - name: jellyfin
      namespace: media
    middlewares:
    - name: rfc1918
  tls:
    secretName: "${SECRET_DOMAIN/./-}"
