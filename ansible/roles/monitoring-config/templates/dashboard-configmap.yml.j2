apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-{{ filename }}
  namespace: monitoring
data:
  {{ filename }}.json: |-
    {{ lookup('file', './dashboards/{{ filename }}.json')| from_json | to_json(ensure_ascii=False) }}
