apiVersion: metal.sidero.dev/v1alpha1
kind: ServerClass
metadata:
  name: default-arm64
spec:
  environmentRef:
    namespace: default
    name: default
  selector:
    matchLabels:
      pi-master: "true"
  configPatches:
    - op: replace
      path: /machine/install/disk
      value: /dev/sda
