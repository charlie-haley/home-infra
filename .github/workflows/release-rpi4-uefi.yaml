---
name: release raspberrypi4-uefi

on:
  push:
    paths:
      - .github/workflows/release-rpi4-uefi.yaml
      - integrations/sidero/custom/raspberrypi4-uefi/**

jobs:
  build-publish:
    runs-on:
      - ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: setup buildx
        id: buildx
        uses: docker/setup-buildx-action@v1

      - name: login ghcr
        uses: docker/login-action@v1
        with:
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}
          registry: ghcr.io

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: |
            ghcr.io/charlie-haley/raspberrypi4-uefi
          tags: |
            type=sha

      - name: build and push to ghcr
        uses: docker/build-push-action@v3
        with:
          push: ${{ github.ref == 'refs/heads/main' }}
          context: ./integrations/sidero/custom/raspberrypi4-uefi
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
